pub mod components;

use bevy::prelude::*;

pub struct FoodPlugin;

impl Plugin for FoodPlugin {
    fn build(&self, _app: &mut App) {
        // Phase 2: food spawning, pickup, throwing, trajectories
    }
}
